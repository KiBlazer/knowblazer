# Recall

Generated task context packs can be written here.

Recall packs are outputs, not source-of-truth memory. They can be useful for reuse, but they should not replace reviewed memory in `profile/`, `projects/`, or `experience/`.
