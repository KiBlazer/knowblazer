# Automatic Experience

Newly captured reusable lessons go here.

Knowblazer should periodically consolidate these into concise, higher-signal `experience/` or `projects/` memory and keep recall focused on the best current context.
